version: "3.9"

services:
  web:
    build: .
    container_name: casas-prefabricadas
    restart: always
    ports:
      - "3000:3000"
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
